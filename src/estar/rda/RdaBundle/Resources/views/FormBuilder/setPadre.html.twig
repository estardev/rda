{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Imposta Padre</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        {{ form(backForm) }}
    </li>
</ul>
{% endblock %}
