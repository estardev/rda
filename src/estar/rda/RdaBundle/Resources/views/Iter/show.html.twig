{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Iter</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Dataora</th>
                <td>{{ entity.dataora|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Dastato</th>
                <td>{{ entity.dastato }}</td>
            </tr>
            <tr>
                <th>Astato</th>
                <td>{{ entity.astato }}</td>
            </tr>
            <tr>
                <th>Motivazione</th>
                <td>{{ entity.motivazione }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

<div class="bottoniera">
        <a class="btn btn-default" href="{{ path('iter') }}">
Indietro        </a>

        <a class="btn btn-default" href="{{ path('iter_edit', { 'id': entity.id }) }}">
            Edit
        </a>

    <!-- <li>{{ form(delete_form) }}</li> -->
</div>
{% endblock %}
