{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Nuova Area</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('area') }}">
            Indietro alla Lista
        </a>
    </li>
</ul>
{% endblock %}
