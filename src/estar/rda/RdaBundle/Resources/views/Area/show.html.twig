{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Area</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Nome</th>
                <td>{{ entity.nome }}</td>
            </tr>
            <tr>
                <th>Descrizione</th>
                <td>{{ entity.descrizione }}</td>
            </tr>
            <tr>
                <th>Gruppogestav</th>
                <td>{{ entity.gruppogestav }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('area') }}">
            Indietro alla Lista
        </a>
    </li>
    <li>
        <a href="{{ path('area_edit', { 'id': entity.id }) }}">
            Modifica
        </a>
    </li>
    <!-- <li>{{ form(delete_form) }}</li> -->
</ul>
{% endblock %}
