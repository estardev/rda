{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Utentegruppoutente list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Amministratore</th>
                <th>Idutentegruppoutente</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('utentegruppoutente_show', { 'id': entity.idutentegruppoutente }) }}">{{ entity.amministratore }}</a></td>
                <td>{{ entity.idutentegruppoutente }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('utentegruppoutente_show', { 'id': entity.idutentegruppoutente }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('utentegruppoutente_edit', { 'id': entity.idutentegruppoutente }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('utentegruppoutente_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
