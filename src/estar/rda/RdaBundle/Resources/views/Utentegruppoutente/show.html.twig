{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Utentegruppoutente</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Amministratore</th>
                <td>{{ entity.amministratore }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

<div class="bottoniera">
        <a class="btn btn-default" href="{{ path('utentegruppoutente') }}">
            Torna indietro
        </a>
        <a class="btn btn-default" href="{{ path('utentegruppoutente_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    <!-- <li>{{ form(delete_form) }}</li> -->
</div>
{% endblock %}
