{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Richiestadocumento list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Datainvio</th>
                <th>Numeroprotocollo</th>
                <th>Filepath</th>
                <th>Idrichiestadocumento</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('richiestadocumento_show', { 'id': entity.idrichiestadocumento }) }}">{{ entity.datainvio }}</a></td>
                <td>{{ entity.numeroprotocollo }}</td>
                <td>{{ entity.filepath }}</td>
                <td>{{ entity.idrichiestadocumento }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('richiestadocumento_show', { 'id': entity.idrichiestadocumento }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('richiestadocumento_edit', { 'id': entity.idrichiestadocumento }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('richiestadocumento_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
