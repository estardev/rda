{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Richiestadocumento list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Datainvio</th>
                <th>Numeroprotocollo</th>
                <th>Filepath</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('richiestadocumento_show', { 'id': entity.id }) }}">{{ entity.datainvio }}</a></td>
                <td>{{ entity.numeroprotocollo }}</td>
                <td>{{ entity.filepath }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('richiestadocumento_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('richiestadocumento_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('richiestadocumento_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
