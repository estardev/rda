{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Richiestadocumento creation</h1>

    {{ form(form, {'attr': {'novalidate': 'novalidate'}}) }}

<div class="bottoniera">
        <a class="btn btn-default" href="{{ path('richiestadocumento') }}">
            Torna indietro
        </a>
</div>
{% endblock %}
