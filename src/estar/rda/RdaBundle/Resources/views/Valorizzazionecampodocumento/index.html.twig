{% extends '::base.html.twig' %}

{% block body -%}
<div class="panel panel-default">
    <div class="panel-heading"><h4>Lista Valorizzaione Campo-Documento</h4></div>

    <table class="table">
        <thead>
            <tr>
                <th>Valore</th>
                <th>Id</th>
                <th>Azioni</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('valorizzazionecampodocumento_show', { 'id': entity.id }) }}">{{ entity.valore }}</a></td>
                <td>{{ entity.id }}</td>
                <td>
                    <div class="btn-group" role="group">
                        <button type="button" class="btn btn-default"><span class="glyphicon glyphicon-eye-open" aria-hidden="true"></span><a href="{{ path('valorizzazionecampodocumento_show', { 'id': entity.id }) }}"> Visualizza</a></button>
                        <button type="button" class="btn btn-default"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span><a href="{{ path('valorizzazionecampodocumento_edit', { 'id': entity.id }) }}"> Modifica</a></button>

                    </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>

    {% endblock %}

