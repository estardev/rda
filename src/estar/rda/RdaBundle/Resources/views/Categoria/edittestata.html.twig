{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Dati di testata della Categoria</h1>

    {{ form(edit_form) }}

<div class="bottoniera">
        <a class="btn btn-default" href="{{ path('area') }}">
            Indietro alla lista
        </a>
    <!-- <li>{{ form(delete_form) }}</li> -->
</div>
{% endblock %}
