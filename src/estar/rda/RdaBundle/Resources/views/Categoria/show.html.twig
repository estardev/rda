{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Categoria</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Nome</th>
                <td>{{ entity.nome }}</td>
            </tr>
            <tr>
                <th>Descrizione</th>
                <td>{{ entity.descrizione }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>
    <br>
    &nbsp;
    <div class="btn-group">
        <a class="btn btn-default btn-sm"  href="{{ path('categoria') }}">
            Torna Indietro
        </a>
        <a class="btn btn-default btn-sm"  href="{{ path('formbuilder_showByCategoria', { 'idCategoria': entity.id }) }}">
            Modifica
        </a>
    </div>
    {#
        <ul class="record_actions">
    <li>
        <a href="{{ path('categoria') }}">
            Torna indietro
        </a>
    </li>
    <li>
        <a href="{{ path('formbuilder_showByCategoria', { 'idCategoria': entity.id }) }}">
            Edit
        </a>
    </li>
   <!-- <li>{{ form(delete_form) }}</li> -->

</ul>
#}
{% endblock %}
