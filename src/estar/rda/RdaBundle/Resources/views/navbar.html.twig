<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-collapse-1">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        &nbsp;
    </div>
    <div class="navbar-left navbar-brand" href="#"><a class="navbar-brand">RDA Online</a></div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="navbar-collapse-1">

        <form class="navbar-form navbar-left" role="search">
            <div class="form-group">
                <input type="text" class="form-control" placeholder="Cerca">
            </div>
            <button type="submit" class="btn btn-default"><i class="glyphicon glyphicon-search"></i></button>
        </form>

        <form class="navbar-form navbar-left">
            <div class="form-group">
                <select id="homepageSelectCategoria" class="form-control">
                    {% for entity in categoria %}
                        <option value="{{ entity.id }}" type="text">{{ entity.descrizione }}</option>
                    {% endfor %}
                </select>
            </div>
            <button type="button" id="homepageSelectBtnCategoria" class="btn btn-default" >Nuova</button>
        </form>

        <ul class="nav navbar-nav navbar-right">

            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                    {#<b>Benvenuto {{ utente }}</b>#}
                    {#{% if is_granted("ROLE") %}#}
                    Benvenuto, <b>{{ app.user.username|upper }}</b>
                    {#{% endif %}#}
                    <span class="caret"></span>
                </a>
                <ul class="dropdown-menu">
                    <li><a href="./profile">Visualizza Utente</a></li>
                    <li class="divider"></li>
                    <li><a href="./profile/change-password">Cambio Password</a></li>
                    <li class="divider"></li>
                    <li><a href="./logout">Logout</a></li>
                </ul>
            </li>
        </ul>

    </div><!-- /.navbar-collapse -->
</nav>