{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Utente</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Utenteldap</th>
                <td>{{ entity.utenteldap }}</td>
            </tr>
            <tr>
                <th>Utentecartaoperatore</th>
                <td>{{ entity.utentecartaoperatore }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

<div class="bottoniera">
        <a class="btn btn-default" href="{{ path('utente') }}">
            Torna indietro
        </a>
        <a class="btn btn-default" href="{{ path('utente_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    <!-- <li>{{ form(delete_form) }}</li> -->
</div>
{% endblock %}
