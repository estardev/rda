{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Utente list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Utenteldap</th>
                <th>Utentecartaoperatore</th>
                <th>Idutente</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('utente_show', { 'id': entity.idutente }) }}">{{ entity.utenteldap }}</a></td>
                <td>{{ entity.utentecartaoperatore }}</td>
                <td>{{ entity.idutente }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('utente_show', { 'id': entity.idutente }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('utente_edit', { 'id': entity.idutente }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('utente_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
