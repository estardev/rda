{% extends '::base.html.twig' %}

{% block body -%}
<div class="panel panel-default">
    <div class="panel-heading"><h4>Elenco utenti configurati nel sistema</h4></div>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nome e Cognome</th>
                <th>Utenteldap</th>
                <th>Utentecartaoperatore</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.id }}</td>
                <td>{{ entity.nomecognome }}</td>
                <td><a href="{{ path('utente_show', { 'id': entity.id }) }}">{{ entity.utenteldap }}</a></td>
                <td>{{ entity.utentecartaoperatore }}</td>

                <td>
                    <div class="btn-group" role="group">
                        <button type="button" class="btn btn-default"><span class="glyphicon glyphicon-eye-open" aria-hidden="true"></span><a href="{{ path('utente_show', { 'id': entity.id }) }}">Mostra</a></button>
                        <button type="button" class="btn btn-default"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span><a href="{{ path('fos_user_profile_edit', { 'idUtente': entity.id }) }}">Modifica</a></button>

                </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

</div>
    {% endblock %}
