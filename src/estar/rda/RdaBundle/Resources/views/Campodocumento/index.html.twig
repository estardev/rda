{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Campodocumento list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Nome</th>
                <th>Tipo</th>
                <th>Idcampodocumento</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('campodocumento_show', { 'id': entity.idcampodocumento }) }}">{{ entity.nome }}</a></td>
                <td>{{ entity.tipo }}</td>
                <td>{{ entity.idcampodocumento }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('campodocumento_show', { 'id': entity.idcampodocumento }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('campodocumento_edit', { 'id': entity.idcampodocumento }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('campodocumento_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
