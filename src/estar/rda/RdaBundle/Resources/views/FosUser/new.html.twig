{% extends '::base.html.twig' %}

{% block body -%}
    <h1>FosUser creation</h1>

    {{ form(form, {'attr': {'novalidate': 'novalidate'}}) }}

<div class="bottoniera">
        <a class="btn btn-default" href="{{ path('fosuser') }}">
            Torna indietro
        </a></div>

{% endblock %}
