{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Azienda creation</h1>

    {{ form(form, {'attr': {'novalidate': 'novalidate'}}) }}


<div class="bottoniera">
        <a class="btn btn-default" href="{{ path('azienda') }}">
            Indietro alla lista
        </a>
</div>
{% endblock %}
