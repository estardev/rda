{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Azienda edit</h1>

    {{ form(edit_form) }}
    {{ form(delete_form) }}

    <button type="button" class="btn btn-default"><a href="{{ path('azienda') }}">
            <span class="glyphicon glyphicon-chevron-left"></span> Ritorna alla lista Azienda
        </a></button>


{% endblock %}
