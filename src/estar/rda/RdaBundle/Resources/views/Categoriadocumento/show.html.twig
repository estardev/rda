{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Categoriadocumento</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Necessarioperabs</th>
                <td>{{ entity.necessarioperabs }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

    <div class="bottoniera">
        <a class="btn btn-default" href="{{ path('categoriadocumento') }}">
indietro        </a>

        <a class="btn btn-default" href="{{ path('categoriadocumento_edit', { 'id': entity.id }) }}">
            Edit
        </a>

    <!-- <li>{{ form(delete_form) }}</li> -->
    </div>
{% endblock %}
