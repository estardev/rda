{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Categoriadocumento creation</h1>

    {{ form(form, {'attr': {'novalidate': 'novalidate'}}), {'attr': {'novalidate': 'novalidate'}}) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('categoriadocumento') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
