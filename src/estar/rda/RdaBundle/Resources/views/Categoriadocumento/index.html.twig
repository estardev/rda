{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Categoriadocumento list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Necessarioperabs</th>
                <th>Idcategoriadocumento</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('categoriadocumento_show', { 'id': entity.idcategoriadocumento }) }}">{{ entity.necessarioperabs }}</a></td>
                <td>{{ entity.idcategoriadocumento }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('categoriadocumento_show', { 'id': entity.idcategoriadocumento }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('categoriadocumento_edit', { 'id': entity.idcategoriadocumento }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('categoriadocumento_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
