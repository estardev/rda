{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Categoriagruppo list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Abilitatoinserimentorichieste</th>
                <th>Validatoretecnico</th>
                <th>Validatoreamministrativo</th>
                <th>Referenteabs</th>
                <th>Idcategoriagruppo</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('categoriagruppo_show', { 'id': entity.idcategoriagruppo }) }}">{{ entity.abilitatoinserimentorichieste }}</a></td>
                <td>{{ entity.validatoretecnico }}</td>
                <td>{{ entity.validatoreamministrativo }}</td>
                <td>{{ entity.referenteabs }}</td>
                <td>{{ entity.idcategoriagruppo }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('categoriagruppo_show', { 'id': entity.idcategoriagruppo }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('categoriagruppo_edit', { 'id': entity.idcategoriagruppo }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('categoriagruppo_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
