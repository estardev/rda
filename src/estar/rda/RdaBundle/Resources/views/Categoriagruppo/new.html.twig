{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Creazione di un nuovo permesso</h1>

    {{ form(form, {'attr': {'novalidate': 'novalidate'}}) }}

        <ul class="record_actions">
            <div class="bottoniera">

        <a class="btn btn-default" href="{{ path('categoriagruppo') }}">
            Indietro alla lista
        </a>

                </div>
</ul>
{% endblock %}
