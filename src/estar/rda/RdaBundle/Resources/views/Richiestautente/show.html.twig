{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Richiestautente</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Creatore</th>
                <td>{{ entity.creatore }}</td>
            </tr>
            <tr>
                <th>Validatoretecnico</th>
                <td>{{ entity.validatoretecnico }}</td>
            </tr>
            <tr>
                <th>Validatoreamministrativo</th>
                <td>{{ entity.validatoreamministrativo }}</td>
            </tr>
            <tr>
                <th>Referenteabs</th>
                <td>{{ entity.referenteabs }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

<div class="bottoniera">
        <a class="btn btn-default" href="{{ path('richiestautente') }}">
            Torna indietro
        </a>

        <a class="btn btn-default" href="{{ path('richiestautente_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    <!-- <li>{{ form(delete_form) }}</li> -->
</div>
{% endblock %}
