{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Richiestautente creation</h1>

    {{ form(form, {'attr': {'novalidate': 'novalidate'}}) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('richiestautente') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
