{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Richiestautente list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Creatore</th>
                <th>Validatoretecnico</th>
                <th>Validatoreamministrativo</th>
                <th>Referenteabs</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('richiestautente_show', { 'id': entity.id }) }}">{{ entity.creatore }}</a></td>
                <td>{{ entity.validatoretecnico }}</td>
                <td>{{ entity.validatoreamministrativo }}</td>
                <td>{{ entity.referenteabs }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('richiestautente_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('richiestautente_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('richiestautente_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
